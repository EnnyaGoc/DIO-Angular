// require -> para importar arquivo

const funcoes = require('./funcoes-auxiliares');

/* const { gets, print } = require('./funcoes-auxiliares');
criou 2 variaveis que recebe o objeto q tem o get e print  

print(gets());
*/

//console.log(funcoes)

//console.log(funcoes.gets()) retorna 10

const pessoa = {
    nome: 'vitor'
};

/* const { nome } = pessoa;
sao equivalentes
const nome = pessoa.nome; */

console.log({nome})